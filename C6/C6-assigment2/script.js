class Students {
    constructor(name,phone_number,age,) {
        this.hobbies = "";
        this.name = name
        this.phone_number = phone_number
        this.age = age
    }

    setHobbies( hobbies ) {
       this.hobbies = hobbies
   }

   sayMyHobbies () {
      return `I like ${this.hobbies}`;
     }

    sayMyName () {
        return `Hello I am a student, and I am ${this.name}`;
    }

    sayMyPhone_Number () {
        return `My phone_number is ${this.phone_number}`;
    }
    sayMyAge () {
        return `I am ${this.age} years old.`;
    }
}


let allStudent = [];
let Student = ["Sharleen Rollo","Della Wally",];

for(i = 0; i <= student.length; i++) {

    let student = new Students(students[i], 25);
    allStudent[allStudent.length] = student;
    phone_number = new Object(`074982...`[i]);
}
for( j = 0; j < allStudents.length; j++ ) {
    console.log(allStudent[j].sayMyName());
    console.log(allStudent[j].sayMyPhone_Number());
    console.log(allStudent[j].sayMyAge());
    console.log(allStudent[j].sayMyHobbies());
}

let Student2 = [ "Ryana Ami"];

for(i = 0; i <= student.length; i++) {
    
    let student = new Students(students[i], 23);
    allStudent[allStudent.length] = student;
    phone_number = new Object(`074952...`[i]);
}

for ( j = 0; j < allStudents.length; j++) {
    console.log(allStudent[j].sayMyName());
    console.log(allStudent[j].sayMyPhone_Number());
    console.log(allStudent[j].sayMyAge());
    console.log(allStudent[j].sayMyHobbies());
}
let Student3 = ["Lydia Mercy"]

for(i = 0; i <= student.length; i++) {
    
    let student = new Students(students[i], 25);
    allStudent[allStudent.length] = student;
    phone_number = new Object(`074965...`[i]);
}

for ( j = 0; j < allStudents.length; j++) {
    console.log(allStudent[j].sayMyName());
    console.log(allStudent[j].sayMyPhone_Number());
    console.log(allStudent[j].sayMyAge());
    console.log(allStudent[j].sayMyHobbies());
}

let Student4 = [ "Mikey Valorie", "Chester Lexie", "Danette Luanna"]

for(i = 0; i <= student.length; i++) {

    let student = new Students(students[i], 25);
    allStudent[allStudent.length] = student;
    phone_number = new Object(`074974...`[i]);
}

for ( j = 0; j < allStudents.length; j++) {    
    console.log(allStudent[j].sayMyName());
    console.log(allStudent[j].sayMyPhone_Number());
    console.log(allStudent[j].sayMyAge());
    console.log(allStudent[j].sayMyHobbies());
}